# VirtualBuddy — SERVER role only (the 1050ti brain host).
# The laptop and phone use the light requirements.txt (no torch) and talk to this
# machine over HTTP. Install here ONLY on the box that hosts the brain + does teaching.
#
#   pip install -r requirements.txt          # base app
#   pip install -r requirements-server.txt   # + GPU brain/teaching stack
#
# Notes for the GTX 1050ti (4GB): target small base models (0.5B-1.5B) and let
# Unsloth's memory optimizations + gradient checkpointing keep training in-budget.
# Match the torch build to your installed CUDA (see pytorch.org for the right index-url).

-r requirements.txt

# --- fine-tuning (teach.py) ---
torch>=2.2            # install the CUDA build for your driver from pytorch.org
transformers>=4.44
datasets>=2.20
peft>=0.12           # LoRA adapters
accelerate>=0.33
bitsandbytes>=0.43   # 4-bit (QLoRA)
unsloth              # 2x faster / ~70% less VRAM training; makes 4GB viable for small models

# --- server brain (optional niceties; brain_server.py itself is stdlib-only) ---
# fastapi
# uvicorn
