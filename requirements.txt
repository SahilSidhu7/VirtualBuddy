# Core — everything the buddy needs to route and run skills.
# Routing is hashed n-grams over numpy (vb/textvec.py). scikit-learn used to
# provide that one vectoriser and cost 180MB in the packaged build, so it went.
numpy>=1.24
httpx>=0.27

# Web reading. trafilatura gives clean article text; without it we fall back
# to a plain tag-stripper, so it is nice-to-have rather than required.
trafilatura>=1.8

# Task manager and PC health readings.
psutil>=5.9

# Deleting via the Recycle Bin instead of unlink, so it stays undoable.
send2trash>=1.8

# Optional, installed on demand from inside the app:
#   playwright        tier-2 browser for JS-heavy sites and clicking
#   vosk, sounddevice voice input
#   pyttsx3           spoken replies
