# Core (small, needed)
pyyaml
numpy
scikit-learn      # trains the local intent classifier
joblib
pyttsx3           # offline text-to-speech
psutil            # system status, agents, serving
pillow            # screenshots + character sprites
pywebview         # modern dashboard window (uses the OS webview; tkinter fallback)
playwright        # web automation primitives (open/read/click/fill); browser bundled in release
pyperclip         # clipboard skills
pygetwindow       # "what's open", switch/close/minimize windows

# Intent embeddings come from Ollama (nomic-embed-text) - no torch needed.
# Optional heavier fallback if you don't want to use Ollama for embeddings:
# sentence-transformers

# Voice input (optional, for --voice)
vosk
sounddevice
# Model: installer --voice downloads vosk-model-small-en-us-0.15 -> models/vosk

# Character overlay + web search use built-in tkinter / urllib (no install)
